name: embeddle
description: Guess the word, using the embedding distance

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  langchain_openai: ^0.7.2+2
  langchain: ^0.7.7+1
  flutter_dotenv: ^5.1.0
  word_generator: ^0.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
   - .env
